{
  "name": "any-match",
  "description": "Match a value against an Array of strings/numbers/regexes.",
  "version": "1.0.1",
  "license": "MIT",
  "author": "Steven Vachon <contact@svachon.com> (https://www.svachon.com/)",
  "repository": "stevenvachon/any-match",
  "dependencies": {
    "is-regexp": "^1.0.0"
  },
  "devDependencies": {
    "chai": "^4.0.0",
    "mocha": "^3.4.2"
  },
  "engines": {
    "node": ">= 4"
  },
  "scripts": {
    "test": "mocha test --reporter=spec --check-leaks --bail"
  },
  "files": [
    "index.js"
  ],
  "keywords": [
    "match",
    "regex",
    "regexp"
  ]
}
