{
  "name": "any-match",
  "description": "Determine if a single match exists with an array of strings/numbers/regexes.",
  "version": "2.0.0",
  "license": "MIT",
  "author": "Steven Vachon <contact@svachon.com> (https://www.svachon.com/)",
  "repository": "stevenvachon/any-match",
  "dependencies": {
    "is-regexp": "^2.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^4.1.0"
  },
  "engines": {
    "node": ">= 6"
  },
  "scripts": {
    "test": "mocha test --check-leaks --bail"
  },
  "files": [
    "index.js"
  ],
  "keywords": [
    "match",
    "regex",
    "regexp"
  ]
}
